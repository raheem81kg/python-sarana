SARANA PARSE TREE DIAGRAMS
======================================================================
Generated by: generate_parse_tree.py

A parse tree shows the hierarchical structure of a program
as derived from the grammar rules. Each node represents either
a grammar rule or a terminal symbol (token).

======================================================================
EXAMPLE 1: Simple Expression (PEMDAS)
======================================================================

Sarana Code: bloom C = A + B * B;

Parse Tree:
└── Program
    └── BloomStatement "C"
        ├── name: "C"
        └── value:
            └── BinaryOp "+"
                ├── Variable "A"
                └── BinaryOp "*"
                    ├── Variable "B"
                    └── Variable "B"

Key Observations:
- The "+" operator is the root of the expression
- The "*" operator is deeper in the tree (higher precedence)
- This structure enforces PEMDAS: C = A + (B * B)

======================================================================
EXAMPLE 2: Required Assignment Sample
======================================================================

Sarana Code:
bloom A = 20;
bloom B = 40;
bloom C = A + B * B;
try {
    bloom D = C / 0;
}
ketch {
    echo "Error: Division by zero attempted but not allowed.";
}
echo "The result is " C;

Parse Tree:
└── Program
    ├── BloomStatement "A"
    │   ├── name: "A"
    │   └── value:
    │       └── Integer = 20
    ├── BloomStatement "B"
    │   ├── name: "B"
    │   └── value:
    │       └── Integer = 40
    ├── BloomStatement "C"
    │   ├── name: "C"
    │   └── value:
    │       └── BinaryOp "+"
    │           ├── Variable "A"
    │           └── BinaryOp "*"
    │               ├── Variable "B"
    │               └── Variable "B"
    ├── TryKetchStatement
    │   ├── try:
    │   │   └── BloomStatement "D"
    │   │       ├── name: "D"
    │   │       └── value:
    │   │           └── BinaryOp "/"
    │   │               ├── Variable "C"
    │   │               └── Integer = 0
    │   └── ketch:
    │       └── EchoStatement
    │           └── String = Error: Division by zero attempted but not allowed.
    └── EchoStatement
        ├── String = The result is 
        └── Variable "C"

Key Observations:
- Program has 5 top-level statements
- Three BloomStatements (A, B, C)
- One TryKetchStatement with nested blocks
- One EchoStatement with multiple expressions

======================================================================
EXAMPLE 3: Function with Conditional
======================================================================

Sarana Code:
craft max(a, b) {
    when (a > b) {
        return a;
    } otherwise {
        return b;
    }
}

Parse Tree:
└── Program
    └── CraftStatement "max"
        ├── params:
        │   ├── str
        │   └── str
        └── body:
            └── WhenStatement
                ├── condition:
                │   └── BinaryOp ">"
                │       ├── Variable "a"
                │       └── Variable "b"
                ├── then:
                │   └── ReturnStatement = Variable('a')
                │       └── Variable "a"
                └── otherwise:
                    └── ReturnStatement = Variable('b')
                        └── Variable "b"

Key Observations:
- CraftStatement defines a function
- Function body contains a WhenStatement
- WhenStatement has both "then" and "otherwise" branches
- Each branch contains a ReturnStatement

